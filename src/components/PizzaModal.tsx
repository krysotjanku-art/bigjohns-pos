import type { MenuItem, PizzaSize } from "../types/menu";
interface Props { pizza: MenuItem | null; onClose: () => void; onSizeSelect: (pizza: MenuItem, size: PizzaSize) => void; }
const sizes: readonly PizzaSize[] = [{ label: "Small", priceOffset: 0, idOffset: 0 }, { label: "Medium", priceOffset: 40, idOffset: 100 }, { label: "XL", priceOffset: 80, idOffset: 200 }];
export function PizzaModal({ pizza, onClose, onSizeSelect }: Props) { if (!pizza) return null; return <div style={{ position: "fixed", inset: 0, background: "rgba(0,0,0,0.5)", display: "flex", justifyContent: "center", alignItems: "center" }}><div style={{ background: "white", padding: 30, borderRadius: 15, textAlign: "center" }}><h2>{pizza.nazev}</h2><p>Vyber velikost:</p>{sizes.map((size) => <button key={size.label} type="button" onClick={() => onSizeSelect(pizza, size)} style={{ width: 120, height: 50, margin: 10 }}>{size.label}</button>)}<br /><button type="button" onClick={onClose} style={{ marginTop: 20, width: 120, height: 45 }}>Zavřít</button></div></div>; }
