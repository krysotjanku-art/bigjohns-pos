import {useState}from"react";interface Props{onSubmit:(p:string)=>boolean;onCancel:()=>void;}export function PinDialog({onSubmit,onCancel}:Props){const[p,setP]=useState("");const[e,setE]=useState("");return <div style={{position:"fixed",inset:0,background:"rgba(0,0,0,.5)",display:"grid",placeItems:"center"}}><div style={{background:"white",padding:24}}><h2>Administrátorský PIN</h2><input autoFocus type="password" value={p} onChange={x=>setP(x.target.value)}/><button onClick={()=>onSubmit(p)?null:setE("Nesprávný PIN.")}>Potvrdit</button><button onClick={onCancel}>Zrušit</button>{e&&<p>{e}</p>}</div></div>}
