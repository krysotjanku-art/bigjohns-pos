import { useState } from "react";
import "./PinDialog.css";
interface Props { onSubmit: (pin: string) => boolean; onCancel: () => void; }
export function PinDialog({ onSubmit, onCancel }: Props) { const [pin, setPin] = useState(""); const [error, setError] = useState(""); return <div className="pin-dialog__backdrop"><section className="pin-dialog" role="dialog" aria-modal="true"><h2>Administrátorský PIN</h2><p>Pro pokračování zadejte PIN.</p><input autoFocus inputMode="numeric" type="password" value={pin} onChange={(event) => setPin(event.target.value)} /><div className="pin-dialog__actions"><button type="button" onClick={onCancel}>Zrušit</button><button type="button" onClick={() => onSubmit(pin) ? null : setError("Nesprávný PIN.")}>Potvrdit</button></div>{error && <p className="pin-dialog__error">{error}</p>}</section></div>; }
