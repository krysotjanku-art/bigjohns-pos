import {orderItemKey} from "../order";import type {OrderItem} from "../types/menu";import "./PosUi.css";
interface Props{items:readonly OrderItem[];total:number;onIncrement:(key:string)=>void;onDecrement:(key:string)=>void;onRemove:(key:string)=>void;onPay:()=>void}
export function OrderPanel({items,total,onIncrement,onDecrement,onRemove,onPay}:Props){return <aside className="order-panel"><h2>🧾 Objednávka</h2><div className="order-panel__items">{items.map(item=>{const key=orderItemKey(item);return <div className="order-line" key={key}><div className="order-line__details"><span className="order-line__name">{item.nazev}</span><div className="order-line__meta"><span>× {item.pocet}</span><strong>{item.cena*item.pocet} Kč</strong></div></div><div className="order-line__controls"><button aria-label="Odebrat jednu položku" onClick={()=>onDecrement(key)}>−</button><button className="order-line__increase" aria-label="Přidat jednu položku" onClick={()=>onIncrement(key)}>+</button><button className="order-line__remove" aria-label="Odstranit položku" onClick={()=>onRemove(key)}>🗑</button></div></div>})}</div><footer><div>Celkem <strong>{total} Kč</strong></div><small>Včetně DPH</small><button onClick={onPay}>Zaplatit</button></footer></aside>}
