import{describe,expect,it}from"vitest";import{paymentMethodLabel}from"./paymentMethod";import{createReceiptSnapshot}from"./receiptSnapshot";import type{OrderItem}from"./types/menu";const item:OrderItem={id:1,nazev:"Položka",cena:10,kategorie:"Dezerty",pocet:1,vatRate:12};describe("payment methods",()=>{it("stores cash as the current receipt payment method",()=>expect(createReceiptSnapshot([item],1,1,new Date()).paymentMethod).toBe("cash"));it("keeps receipt labels extensible",()=>{expect(paymentMethodLabel("cash")).toBe("Hotově");expect(paymentMethodLabel("card")).toBe("Kartou")})});
