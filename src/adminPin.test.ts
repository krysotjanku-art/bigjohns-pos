import{describe,expect,it}from"vitest";import{PIN_KEY,changePin,loadPin,validPin}from"./adminPin";const storage=()=>{const data=new Map<string,string>();return{getItem:(key:string)=>data.get(key)??null,setItem:(key:string,value:string)=>data.set(key,value)}};describe("admin PIN",()=>{it("uses 8848 as the default PIN",()=>expect(loadPin(storage())).toBe("8848"));it("requires exactly four numeric digits",()=>{expect(validPin("1234")).toBe(true);expect(validPin("123")).toBe(false);expect(validPin("12345")).toBe(false);expect(validPin("12ab")).toBe(false)});it("changes and persists a valid PIN",()=>{const local=storage();expect(changePin(local,"8848","2468","2468")).toBeNull();expect(local.getItem(PIN_KEY)).toBe("2468");expect(loadPin(local)).toBe("2468")});it("keeps the old PIN after validation errors",()=>{const local=storage();expect(changePin(local,"0000","2468","2468")).toContain("Současný");expect(changePin(local,"8848","246","246")).toContain("přesně");expect(changePin(local,"8848","2468","1357")).toContain("neshodují");expect(loadPin(local)).toBe("8848")})});
