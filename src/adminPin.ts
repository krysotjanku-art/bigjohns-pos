export const PIN_KEY="bigjohns.admin-pin"; export const loadPin=(s:Pick<Storage,"getItem">)=>s.getItem(PIN_KEY)??"1234"; export const validPin=(pin:string)=>/^\d{4,}$/.test(pin); export const changePin=(s:Pick<Storage,"getItem"|"setItem">,current:string,next:string,confirm:string)=>{if(current!==loadPin(s))return"Aktuální PIN není správný.";if(!validPin(next))return"Nový PIN musí mít alespoň 4 číslice.";if(next!==confirm)return"Nový PIN a potvrzení se neshodují.";s.setItem(PIN_KEY,next);return null;};
