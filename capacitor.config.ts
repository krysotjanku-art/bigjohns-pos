import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "cz.bigjohns.pos",
  appName: "Big John's POS",
  webDir: "dist",
  android: {
    backgroundColor: "#171b21",
  },
};

export default config;
